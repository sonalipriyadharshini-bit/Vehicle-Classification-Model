# Vehicle-Classification-Model
Vehicle type classification using transfer learning (ResNet18) in PyTorch — classifies images into 4 categories (Car, Bus, Truck, Motorcycle), trained for 10 epochs with full evaluation metrics (accuracy, precision, recall, F1-score, confusion matrix). Includes both local and Google Colab pipelines.
